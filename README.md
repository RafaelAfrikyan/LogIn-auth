## Usage

```js
1. Create .env file in root
   - Add variable such exists in .env.example
     - Pass to that variable right API without the word after the last slash
      - You can see request in src/api/auth
```
